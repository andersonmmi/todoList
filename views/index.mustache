<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>To Do List</title>
    <!-- <link rel="stylesheet" type="text/css" href="/css/styles.css" /> -->
    <link rel="stylesheet" type="text/css" href="css/styles.css" />
  </head>
  <body>
    <p>Test</p>
    <form class="todoList" action="/" method="post">
      <input type="text" name="todo" value="" placeholder="Add todos here" autocomplete="on">
      <button type = "submit">Add Todo</button>
      <div class="todoItems">
        {{#todos}}
          <form class="todoLine" action="/check" method="post">
            <button type="button" name={{.}}>Mark Complete</button>
            <label for={{.}}>{{.}}</label>
          </form>
        {{/todos}}
      <p>Completed Tasks</p>
        {{#todones}}
          <li>{{.}}</li>
        {{/todones}}
      </div>

    </form>
    <!-- more fun with sockets -->
    <!-- sockets breaks her because I don't have socket.io.js in my static directory -->
    <!-- <script src="/socket.io/socket.io.js"></script>
    <script>var socket = io();</script> -->
  </body>
</html>
